# Run with the base compose.yaml; paths resolve relative to this backend directory.
services:
  proxy:
    volumes:
      - ../formfix-frontend/apps/web/dist:/usr/share/nginx/html:ro
      - ./deploy/nginx-frontend.conf:/etc/nginx/conf.d/default.conf:ro
